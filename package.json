{

    "name": "Kite Runner website",

    "version": "1.0.0",

    "devDependencies": { "tailwindcss": "^3.3.5" },

    "scripts": {
        "build": "tailwindcss -i styles.css --minify -o dist/output.css",
        "dev": "tailwindcss -i styles.css -o dist/output.css --watch"
    }

}
